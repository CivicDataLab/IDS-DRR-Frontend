{
  "name": "ids-drr-branding",
  "version": "0.0.0-stub",
  "description": "Empty stub so the frontend builds even without real branding. Deployments wanting branded content install their own implementation (e.g., `npm install --no-save file:../ids-drr-branding`).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "private": true,
  "sideEffects": [
    "**/*.css",
    "**/*.scss"
  ],
  "dependencies": {
    "ids-drr-branding-types": "github:CivicDataLab/ids-drr-branding-types#multi-modules"
  }
}
